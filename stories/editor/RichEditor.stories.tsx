import React from 'react';
import { Meta, Story } from '@storybook/react';
// import { RichEditorProps, RichEditor } from '@src/components/editor';

// controls docs at https://storybook.js.org/docs/react/essentials/controls
/*export default {
  title: 'RichEditor',
  component: RichEditor,
} as Meta<RichEditorProps>;

const Template: Story<RichEditorProps> = (args) => <RichEditor {...args} />;

export const Primary: Story<RichEditorProps> = Template.bind({});
Primary.args = {};*/
